import React from 'react';
import classes from './SlideOutElement.css'

const SlideOutElement = props => (
    <div style={props.prop} className={classes.SlideOutElement}>J</div>
);

export default SlideOutElement;